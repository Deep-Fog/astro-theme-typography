---
import { themeConfig } from '~/.config'
import katexCssUrl from 'katex/dist/katex.min.css?url'

const { latex } = themeConfig

const katex = latex.katex
---

{katex && <link rel="stylesheet" href={katexCssUrl} />}
